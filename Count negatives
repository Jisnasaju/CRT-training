r=int(input(''))
c=int(input(''))

count=0

matrix=[]
for i in range(r):
    matrix.append(list(map(int,input().split())))
 
for row in matrix:
    for num in row:
        if num<0:
            count+=1
print(count)
